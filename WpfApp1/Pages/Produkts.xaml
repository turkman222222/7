<Page x:Class="WpfApp1.Pages.Produkts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Produkts">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            
            <RowDefinition Height="32"/>
            <RowDefinition Height="90"/>
            <RowDefinition Height="58"/>
        </Grid.RowDefinitions>

        <ListView x:Name="listprodukts" Grid.Row="1" ScrollViewer.CanContentScroll="False" Margin="0,45,0,13" Grid.RowSpan="2" Background="#FFFDFDFD" Height="206">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="700"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>

                        

                        <StackPanel Grid.Column="1" Width="Auto" Orientation="Vertical" HorizontalAlignment="Left">
                            <TextBlock Width="Auto" TextWrapping="Wrap" Height="Auto">
                                <Run Text="Категория "/>
                                <Run Text="{Binding id_catekoria}"/>
                            </TextBlock>
                            <TextBlock Width="Auto" TextWrapping="Wrap" Height="Auto">
                                <Run Text="Цена"/>
                                <Run Text="{Binding discount}"/>
                            </TextBlock>
                            <TextBlock Width="Auto" TextWrapping="Wrap" Height="Auto">
                                <Run Text="Количество "/>
                                <Run Text="{Binding quantity}"/>
                            </TextBlock>
                            <TextBlock Width="Auto" TextWrapping="Wrap" Height="Auto">
                                <Run Text="Описание "/>
                                <Run Text="{Binding discription}"/>
                            </TextBlock>
                        </StackPanel>

                       
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="tbCounter" Text="He wahqenor" Grid.Row="2" Width="95" 
                   HorizontalAlignment="Left" Height="22" Margin="695,0,0,0"/>
    </Grid>
</Page>