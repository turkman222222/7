<Page x:Class="WpfApp1.Pages.Produkts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Produkts">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Список продуктов" FontSize="16" FontWeight="Bold" Margin="10"/>

        <!-- ListView с изображениями -->
        <ListView x:Name="listprodukts" Grid.Row="1" Margin="10,29,10,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Изображение -->
                        <Image Grid.Column="0" 
                               Source="{Binding CurrentPhoto}" 
                               Width="80" 
                               Height="80" 
                               Stretch="Uniform"
                               Margin="5"/>

                        <!-- Информация о продукте -->
                        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0">
                            <TextBlock>
                                <Run FontWeight="Bold">Категория:</Run>
                                <Run Text="{Binding kategory.name}"/>
                            </TextBlock>
                            <TextBlock>
                                <Run FontWeight="Bold"> Скидка в %:</Run>
                                <Run Text="{Binding discount}"/>
                            </TextBlock>
                            <TextBlock>
                                <Run FontWeight="Bold">Количество:</Run>
                                <Run Text="{Binding quantity}"/>
                            </TextBlock>
                            <TextBlock TextWrapping="Wrap">
                                <Run FontWeight="Bold">Описание:</Run>
                                <Run Text="{Binding discription}"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Счетчик -->
        <TextBlock x:Name="tbCounter" Grid.Row="2" Margin="10" HorizontalAlignment="Right"/>
        <ComboBox x:Name="discauntcombo" HorizontalAlignment="Left" Margin="10,25,0,0" VerticalAlignment="Top" Width="120" Grid.RowSpan="2" SelectionChanged="discauntcombo_SelectionChanged"/>
        <ComboBox x:Name="catecoriacombo" HorizontalAlignment="Left" Margin="161,25,0,0" Grid.RowSpan="2" VerticalAlignment="Top" Width="120" SelectionChanged="catecoriacombo_SelectionChanged"/>
        <TextBox x:Name="textsearch" HorizontalAlignment="Center" Margin="0,27,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Grid.RowSpan="2" TextChanged="textsearch_TextChanged"/>
    </Grid>
</Page>